# MASt3R-Fusion 代码阅读指南

## 1. 这份指南怎么用

这份文档不是简单的“目录说明”，而是一条可以带你从外到内读完整个工程的路线。

建议你按下面的节奏阅读：

1. 先读“项目边界”和“三条主线”，搞清楚工程到底在做什么。
2. 再读“在线主流程”，把 `main.py` 跑通到脑子里。
3. 然后进入 `mast3r_fusion/`，按数据结构 -> 前端 -> 后端 -> VIO 的顺序深入。
4. 最后再读离线回环和全局优化，理解在线与离线如何拼成完整系统。

如果你只想尽快建立全局印象，优先看：

1. `main.py`
2. `mast3r_fusion/frame.py`
3. `mast3r_fusion/tracker.py`
4. `mast3r_fusion/global_opt.py`
5. `main_loop.py`
6. `main_global_optimization.py`

---

## 2. 项目边界

这个仓库真正需要重点阅读的“自有逻辑”主要集中在三块：

1. 根目录三个入口脚本：
   `main.py`、`main_loop.py`、`main_global_optimization.py`
2. 核心包：
   `mast3r_fusion/`
3. 配置与评估：
   `config/`、`evaluation/`

下面这些目录很重要，但初读时不要陷进去：

1. `thirdparty/mast3r/`
   这是上游模型实现，除非你想改网络本身，否则只需要知道本项目如何调用它。
2. `gtsam/`
   这是大型外部优化库，理解 API 语义即可，不必通读源码。
3. `thirdparty/eigen/`、`thirdparty/in3d/`、`torchcodec/`
   属于底层依赖或工具链，不是本项目算法主线。

一句话概括本工程：

`MASt3R-Fusion = MASt3R 稠密视觉前端 + IMU/GTSAM 后端优化 + 离线回环/全局图优化`

---

## 3. 先建立总图：三条主线

这个工程不是单脚本程序，而是“三阶段流水线”：

### 3.1 在线阶段：`main.py`

职责：

1. 逐帧读图像
2. 用 MASt3R 初始化和跟踪
3. 维护关键帧
4. 在线构建局部视觉边
5. 与 IMU 联合优化
6. 输出轨迹、关键帧 H5、图因子 `graph.pkl`

它解决的是：

- “当前帧在哪里？”
- “当前帧是否要成为新关键帧？”
- “局部窗口内状态怎样实时优化？”

### 3.2 离线回环阶段：`main_loop.py`

职责：

1. 从 `data.h5` 重新加载关键帧
2. 用检索数据库找跨时间候选
3. 对候选做精匹配和小图验证
4. 输出回环边 `graph_loop.pkl`

它解决的是：

- “哪些远距离历史帧其实看到了同一个地方？”

### 3.3 离线全局优化阶段：`main_global_optimization.py`

职责：

1. 合并在线视觉边、IMU 边、回环边、可选 GNSS 边
2. 统一构建大图
3. 多轮 LM 优化
4. 输出最终全局轨迹

它解决的是：

- “把整段轨迹拉直、拉一致、闭环，并对齐到全局坐标”

---

## 4. 一张脑图读懂数据流

建议你先记住下面这条总链路：

```text
原始图像
  -> create_frame()
  -> Frame
  -> mast3r_inference_mono() / mast3r_match_asymmetric()
  -> tracker.track()
  -> SharedKeyframes.append()
  -> FactorGraph.add_factors()
  -> FactorGraph.solve_GN_calib()
  -> 轨迹结果 + data.h5 + graph.pkl
  -> main_loop.py 生成 graph_loop.pkl
  -> main_global_optimization.py 输出最终全局轨迹
```

读代码时，始终盯住四类核心数据：

1. 图像和特征：
   `img`、`feat`、`pos`
2. 稠密几何：
   `X_canon`、`C`
3. 位姿与运动状态：
   `T_WC`、`wTcs`、`ss`、`vs`、`bs`
4. 图因子：
   `ii/jj`、`H/v`、IMU 预积分、回环边

---

## 5. 先读什么：推荐通读顺序

### 第一轮：10 分钟建立轮廓

1. `main.py`
2. `mast3r_fusion/frame.py`
3. `mast3r_fusion/global_opt.py`

目标：

- 知道系统主循环怎么转
- 知道关键帧存了什么
- 知道后端图优化大概在何时触发

### 第二轮：30 分钟打通在线流程

1. `mast3r_fusion/mast3r_utils.py`
2. `mast3r_fusion/matching.py`
3. `mast3r_fusion/tracker.py`
4. `mast3r_fusion/geometry.py`

目标：

- 知道一帧如何变成点图
- 知道两帧如何建立稠密对应
- 知道当前帧如何估计相对关键帧位姿

### 第三轮：深入后端和 VIO

1. `mast3r_fusion/global_opt.py`
2. `mast3r_fusion/vio_utils.py`
3. `mast3r_fusion/geoFunc/data_utils.py`

目标：

- 知道视觉边怎么变成 GTSAM 因子
- 知道 IMU 何时接入、如何初始化
- 知道滑窗边缘化和状态回写怎么做

### 第四轮：离线全局阶段

1. `main_loop.py`
2. `main_global_optimization.py`
3. `evaluation/*.py`

目标：

- 知道回环边怎么产生
- 知道全局图怎样融合在线和离线信息
- 知道结果怎么评估

---

## 6. 你一定要先理解的 6 个数据结构/概念

### 6.1 `Frame`

位置：`mast3r_fusion/frame.py`

它是“单帧的一切视觉状态”的容器，至少要理解这些字段：

| 字段 | 作用 |
| --- | --- |
| `frame_id` | 数据集中的原始帧编号 |
| `img` | 喂给 MASt3R 的归一化图像 |
| `img_true_shape` | 编码/解码时使用的真实图像尺寸 |
| `uimg` | 用于可视化的原图 |
| `T_WC` | 世界到相机的 Sim3 位姿 |
| `X_canon` | 当前帧的稠密 3D 点 |
| `C` | 当前帧每个点的置信度 |
| `feat` | 编码器特征 |
| `pos` | patch 位置编码 |

读法建议：

- 不要把 `Frame` 当作“只存图像”的轻对象，它实际上是视觉前端与后端之间的中间状态载体。

### 6.2 `SharedKeyframes`

位置：`mast3r_fusion/frame.py`

这是跨进程共享的关键帧滑窗缓存。

你要特别注意两个概念：

1. `n_size`
   当前缓冲区里有多少关键帧
2. `rollup_sum`
   已经从共享缓存里滚掉了多少旧关键帧

这意味着：

- 某个关键帧的“全局编号”不等于它在共享张量里的当前位置。

### 6.3 `T_WC`

语义：

- 世界坐标系到相机坐标系的 Sim3 变换

注意点：

1. 它包含尺度，不只是 SE3。
2. 在线早期更多是视觉尺度；VIO 初始化后，尺度才开始被 IMU 校正。

### 6.4 `wTcs` / `ss`

位置：`mast3r_fusion/global_opt.py`

它们是后端优化内部维护的状态表示：

1. `wTcs`
   相机位姿的 `4x4` 矩阵版本
2. `ss`
   独立维护的尺度变量

这套表示是为了更方便和 GTSAM 的 `Pose3 + Double` 对接。

### 6.5 视觉边

在线阶段视觉边不是立刻变成“传统 BA 残差块”，而是先被压成 Hessian 形式。

典型字段：

1. `ii`, `jj`
   哪两个关键帧相连
2. `idx_ii2jj`
   像素匹配索引
3. `valid_match`
   哪些像素对应可信
4. `Q_ii2jj`
   匹配不确定度
5. `H`, `v`
   线性化后的二次项

### 6.6 GTSAM 变量符号

| 符号 | 含义 |
| --- | --- |
| `X(i)` | 第 `i` 帧相机位姿 |
| `S(i)` | 第 `i` 帧尺度 |
| `Z(i)` | 第 `i` 帧 IMU 体位姿 |
| `V(i)` | 第 `i` 帧速度 |
| `B(i)` | 第 `i` 帧 IMU 偏置 |
| `C(i)` | 相机到 IMU 外参 |

这张表一定要记住，因为它贯穿 `global_opt.py` 和 `main_global_optimization.py`。

---

## 7. 在线阶段怎么读：按调用链追

## 7.1 从 `main.py` 开始

你应该按这个顺序看：

1. 命令行参数和配置加载
2. 数据集与标定初始化
3. `SharedKeyframes` / `SharedStates`
4. `FrameTracker`
5. `FactorGraph`
6. 主循环里的三种 `Mode`
7. `run_backend()`

### 你在 `main.py` 里要回答的 5 个问题

1. 第一帧如何初始化？
2. 一般帧如何跟踪？
3. 何时插入关键帧？
4. 后端何时运行一次？
5. 哪些数据会被保存到 `data.h5` 和 `graph.pkl`？

### 运行模式

`Mode.INIT`

- 用 `mast3r_inference_mono()` 给第一帧生成点图
- 加入关键帧池
- 推入后端优化队列

`Mode.TRACKING`

- 调 `tracker.track(frame)`
- 返回是否插入关键帧
- 必要时切换到重定位模式

`Mode.RELOC`

- 先保守地重建当前帧点图
- 给后续重新建立约束留机会

---

## 8. 视觉前端怎么读

### 8.1 `mast3r_fusion/mast3r_utils.py`

这个文件是“模型适配层”。

核心职责：

1. 加载 MASt3R 模型
2. 缓存编码器特征
3. 用 decoder 生成点图和描述子
4. 封装对称/非对称匹配入口

建议重点看：

1. `mast3r_inference_mono()`
2. `mast3r_match_asymmetric()`
3. `mast3r_match_symmetric()`
4. `resize_img()`

阅读抓手：

- 为什么在线跟踪默认用非对称匹配？
- 为什么回环验证更偏向对称匹配？

### 8.2 `mast3r_fusion/matching.py`

这是“稠密对应关系”真正形成的地方。

匹配分三步理解：

1. 几何粗匹配
   `iter_proj`
2. 遮挡/错误几何过滤
3. 描述子局部精细化
4. 可选亚像素 refinement

读这个文件时要想清楚：

- 为什么不是直接全图描述子暴力匹配？
- 为什么先靠射线几何，再靠描述子补细节？

### 8.3 `mast3r_fusion/tracker.py`

这是在线前端真正的核心文件。

最重要的函数只有一个：

- `track(frame)`

你要看懂它做了什么：

1. 当前帧到关键帧做非对称匹配
2. 过滤置信度不够的观测
3. 根据是否有标定，选择两套残差模型
4. 解一个小规模位姿优化
5. 用结果更新关键帧点图
6. 决定是否插入新关键帧

两套位姿优化：

1. `opt_pose_ray_dist_sim3()`
   无标定时，用射线方向与距离残差
2. `opt_pose_calib_sim3()`
   有标定时，用像素重投影 + 对数深度残差

---

## 9. 后端怎么读：这是工程最难的部分

### 9.1 先理解 `global_opt.py` 的定位

`mast3r_fusion/global_opt.py` 不是单纯“存图结构”的文件，而是同时负责：

1. 收集视觉边
2. 维护后端状态
3. 构建 GTSAM 图
4. 边缘化旧变量
5. 执行联合优化
6. 触发 V-I 初始化
7. 导出离线阶段图因子

所以它是整个工程最值得反复读的文件。

### 9.2 推荐阅读顺序

1. `FactorGraph.__init__`
2. `add_factors`
3. `prep_two_way_edges`
4. `solve_GN_calib`
5. `predict_pose`
6. `solve_VI_init`
7. `save_graph`

### 9.3 `add_factors()` 在做什么

可以概括成：

```text
关键帧对
  -> 对称匹配
  -> 双向质量过滤
  -> 写入视觉边缓存
```

关键点：

1. 这里只是“收集边”，不是最终全局求解。
2. 边以批量张量形式缓存，方便后续统一线性化。

### 9.4 `solve_GN_calib()` 为什么最重要

它完成了在线阶段最核心的事：

1. 决定当前滑窗范围
2. 必要时边缘化旧关键帧
3. 把视觉边变成 GTSAM 因子
4. 加入 IMU 因子和外参因子
5. 运行 LM
6. 把结果回写到关键帧和内部状态

你一定要看清楚两个阶段：

1. 边缘化阶段
   旧变量被压成 `marg_factor`
2. 当前窗口优化阶段
   视觉因子 + IMU 因子 + 外参约束 + 边缘化先验一起优化

### 9.5 `Align2GTSAM_factors()` 的意义

这是整个工程最“桥梁型”的函数。

因为：

1. 视觉前端天然工作在 Sim3 空间
2. GTSAM 更适合处理 `Pose3 + Scale`

所以必须有一步把视觉 Hessian 转换成 GTSAM 能接受的线性容器因子。

如果你只想抓住后端核心思想，就记这一句：

`视觉模块提供二次近似，GTSAM 负责把它和 IMU/外参/GNSS 放进统一优化图。`

---

## 10. VIO 初始化怎么读

位置：`mast3r_fusion/vio_utils.py`

这里主要关注：

- `VisualIMUAlignment()`

它做的是经典视觉惯性初始化三件事：

1. 估计陀螺仪偏置
2. 求重力、速度和尺度
3. 调整世界坐标，使重力方向对齐

阅读建议：

1. 先不要纠结每个矩阵块的具体维度
2. 先抓整体流程
3. 再回到 `global_opt.py` 看它何时被触发、结果如何回写

在线阶段的关键逻辑是：

- 当滑窗里积累到足够关键帧后，调用 `solve_VI_init()`
- `solve_VI_init()` 内部再调 `VisualIMUAlignment()`
- 成功后把系统从“纯视觉”切换到“多传感器联合估计”

---

## 11. 离线回环怎么读

位置：`main_loop.py`

理解这个文件最重要的不是细节，而是“策略”：

1. 在线阶段只做局部检索边
2. 离线阶段才敢搜索远距离候选
3. 候选不是全收，而是要经过几何置信图和局部图优化验证

建议重点看：

1. `load_frame_from_h5`
2. `gen_conf_map_vec`
3. `mini_solve`
4. 主循环里的候选过滤逻辑

你要看懂的问题：

1. 为什么回环不放在线主循环里重做？
2. 为什么要先检索，再做小图验证？

---

## 12. 全局优化怎么读

位置：`main_global_optimization.py`

这个文件是“把在线和离线成果最终合并”的地方。

建议按下面顺序看：

1. 数据读取：`graph.pkl`、`graph_loop.pkl`
2. IMU 参数与预积分参数
3. 可选 GNSS 粗对齐
4. 主循环 `for iiter in range(6)`
5. 结果输出

其中最关键的思想有三个：

1. 在线视觉边沿用 Hessian 形式接入
2. 回环边早期宽松、后期严格筛选
3. GNSS 不是直接绑关键帧，而是通过 IMU 预积分桥接时间差

---

## 13. 配置文件怎么读

位置：`config/`

第一次阅读时，建议只关心下面几组配置：

### 13.1 `dataset`

关心：

1. `subsample`
2. `img_downsample`

它们直接影响：

- 帧率
- 点图密度
- 匹配规模
- 显存占用

### 13.2 `tracking`

关心：

1. `max_iters`
2. `sigma_pixel`
3. `sigma_depth`
4. `match_frac_thresh`
5. `min_match_frac`

它们决定前端位姿估计是否稳、关键帧插入是否积极。

### 13.3 `local_opt`

关心：

1. `C_conf`
2. `Q_conf`
3. `pixel_border`
4. `delta_norm`

这些是视觉后端线性化和匹配过滤的核心阈值。

### 13.4 `ms_opt`

关心：

1. `imu_noise`
2. `window_num`
3. `enable_excalib`
4. `subpixel_factor`

这些决定多传感器联合优化的规模和稳定性。

---

## 14. 读代码时最容易混淆的 8 个点

1. `Frame` 不是普通 RGB 帧，而是视觉状态容器。
2. `SharedKeyframes` 用的是滑窗缓存，编号要区分全局编号和局部位置。
3. `T_WC` 是 Sim3，不是纯 SE3。
4. 在线阶段和离线阶段都在做图优化，但目标不同。
5. 在线阶段的检索边主要是局部共视，不是真正的远距离回环。
6. `graph.pkl` 保存的是图因子中间结果，不是最终全局图对象。
7. `solve_GN_calib()` 名字像纯视觉优化，但里面实际上会接入 IMU、外参、边缘化先验。
8. `main_global_optimization.py` 不是简单“再优化一次”，而是把在线图、回环图、可选 GNSS 图统一起来。

---

## 15. 如果你要真正通读完整工程，请按这张清单执行

### 第一天：只读在线主线

1. `main.py`
2. `mast3r_fusion/frame.py`
3. `mast3r_fusion/mast3r_utils.py`
4. `mast3r_fusion/matching.py`
5. `mast3r_fusion/tracker.py`

完成标准：

- 你能说清“当前帧如何变成关键帧”

### 第二天：读在线后端

1. `mast3r_fusion/global_opt.py`
2. `mast3r_fusion/vio_utils.py`
3. `mast3r_fusion/geometry.py`
4. `mast3r_fusion/geoFunc/data_utils.py`

完成标准：

- 你能说清“视觉边、IMU 边、边缘化先验如何进入同一个优化图”

### 第三天：读离线阶段

1. `main_loop.py`
2. `main_global_optimization.py`
3. `evaluation/*.py`

完成标准：

- 你能说清“为什么在线阶段没有解决的问题，要留到离线阶段处理”

---

## 16. 最后给你的阅读口诀

如果你读着读着开始乱，可以用下面这套口诀强行拉回主线：

1. 先问：这是在线还是离线？
2. 再问：这是前端、后端、还是数据结构？
3. 再问：它操作的是图像、点图、位姿，还是图因子？
4. 最后问：它的输出会被哪个模块接着消费？

只要沿着这四个问题去看，这个工程就不会散。

---

## 17. 逐文件精读索引

这一节可以直接当“打开文件时的阅读提词器”。

| 文件 | 先看什么 | 读完要能回答什么 |
| --- | --- | --- |
| `mast3r_fusion/config.py` | `load_config()` | 配置继承怎么做，为什么这是浅合并 |
| `mast3r_fusion/dataloader.py` | `MonocularDataset`、`Intrinsics.from_calib()`、`load_dataset()` | 图像进入系统前做了哪些缩放、去畸变和时间戳处理 |
| `mast3r_fusion/frame.py` | `Frame`、`SharedStates`、`SharedKeyframes` | 当前帧和关键帧分别存什么，滑窗缓存如何维护 |
| `mast3r_fusion/mast3r_utils.py` | `mast3r_inference_mono()`、`mast3r_match_asymmetric()`、`mast3r_match_symmetric()` | MASt3R 在本工程里到底输出什么，在线/离线为何用不同匹配方式 |
| `mast3r_fusion/matching.py` | `match_iterative_proj()` | 粗匹配、遮挡过滤、描述子 refinement、亚像素 refinement 的顺序是什么 |
| `mast3r_fusion/geometry.py` | `constrain_points_to_ray()`、`act_Sim3()`、`project_calib()` | 为什么要把点约束回射线，残差和雅可比怎么定义 |
| `mast3r_fusion/tracker.py` | `track()` | 当前帧如何相对关键帧求位姿，何时插关键帧 |
| `main.py` | `run_backend()` 和主循环 | 在线阶段数据流如何闭环 |
| `mast3r_fusion/global_opt.py` | `add_factors()`、`solve_GN_calib()`、`solve_VI_init()` | 视觉边、IMU、边缘化先验如何统一进 GTSAM |
| `mast3r_fusion/vio_utils.py` | `VisualIMUAlignment()` | VIO 初始化为什么能恢复尺度与重力 |
| `mast3r_fusion/retrieval_database.py` | `update()`、`add_to_database()` | 检索库如何支持关键帧增量入库和即时查询 |
| `main_loop.py` | `gen_conf_map_vec()`、`mini_solve()` | 回环边为什么不是检索到就直接收下 |
| `main_global_optimization.py` | 主迭代 `for iiter in range(6)` | 全局图如何融合视觉、IMU、回环、GNSS |
| `mast3r_fusion/evaluate.py` | `save_traj()`、`save_reconstruction()` | 系统最终如何导出轨迹和重建结果 |
| `evaluation/evaluate_kitti360.py`、`evaluation/evaluate_subt.py` | `ape()` 和主程序入口 | 结果文件如何变成定量误差指标 |

如果你要边读边记，我建议每打开一个文件，只记三件事：

1. 它的输入来自哪里。
2. 它的核心状态是什么。
3. 它的输出会被谁消费。

---

## 18. 两个最常见的追问

### 18.1 VIO 初始化之后，尺度还会继续优化吗

会。

这个工程里的尺度不是“初始化完就冻结”的一次性量，而是后端状态的一部分。

你可以把流程理解成两步：

1. `mast3r_fusion/vio_utils.py` 的 `VisualIMUAlignment()` 先通过视觉-惯性对齐估计一个尺度初值 `s`
2. `mast3r_fusion/global_opt.py` 再把这个尺度初值写回 `self.ss`

但真正关键的是第二步之后：

- 在线阶段的 `solve_GN_calib()` 会持续把 `S(i)` 放进 GTSAM 变量里优化
- 离线阶段的 `main_global_optimization.py` 也会继续优化 `S(i)`

也就是说：

- VIO 初始化解决的是“从纯视觉尺度跳到惯性一致尺度”
- 后续图优化解决的是“在更多视觉、IMU、回环约束下继续细化尺度”

### 18.2 当前代码真的有在线外参估计吗

代码里有这条路径，但默认配置下通常**没有真正开启**。

关键在于区分两个开关：

1. `use_calib`
   表示“使用已有标定文件中的内参和 `Tic`”
2. `ms_opt.enable_excalib`
   表示“是否允许在线优化相机-IMU 外参”

默认理解方式应该是：

- `use_calib=True`
  系统会读取并使用现成标定
- `enable_excalib=False`
  系统不会真正在线标定外参，而是把 `Tic` 作为强先验固定在图里

对应代码集中在 `mast3r_fusion/global_opt.py`：

1. `initials.insert(C(iii), gtsam.Pose3(self.Tic))`
   外参变量会被放进图里
2. `ExPoseConstraintFactor(Z(iii), X(iii), C(iii), ...)`
   外参参与 IMU 位姿和相机位姿之间的约束
3. `if self.enable_excalib: ... else: ...`
   决定外参是被弱先验约束并允许优化，还是被强先验基本固定
4. `self.Tic = cur_result.atPose3(C(0)).matrix()`
   如果在线外参估计打开，优化结果会回写到当前外参

因此，最准确的结论是：

- 代码能力上：支持在线外参估计
- 默认行为上：通常仍以“固定外参”方式运行
